# PDF Processing Module
Use PyMuPDF for PDF processing.

The system should be able to:
- open PDF
- identify pages
- extract text where available
- extract/render page images when OCR is required
- expose metadata where available
